# AI Travel Planner project instructions

Before making changes, read `docs/PROJECT_PROGRESS.md` completely.

Continue from the recorded next step instead of rebuilding completed work.
Keep the project Web/Android compatible unless the user explicitly changes scope.
Do not implement empty placeholder files merely because they exist; implement only the current milestone.

At the end of every development session:

1. Run relevant formatting, analysis, and tests.
2. Update `docs/PROJECT_PROGRESS.md` with the date, files changed, verification results, decisions, known issues, and exact next step.
3. Preserve previous session entries so the file remains a chronological handoff log.
